s1 = {1,2,3,4}
s2 = {4,5,6,7}

s_union = s1.union(s2)
# union() creates a new set with all unique elements from both sets
print(s_union)

s_intersection = s1.intersection(s2)
# intersection() keeps only elements that appear in both sets
print(s_intersection) 